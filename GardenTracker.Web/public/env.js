// Placeholder — overwritten at container startup by docker-entrypoint.sh.
// During local dev this file is served as-is (empty object), and the app
// falls back to import.meta.env values from your .env.local file.
window.__ENV__ = {}
